# Reports

This directory will contain concise lab-validation reports generated from verified commands, logs, and connectivity tests.

Reports must use lab-only data and should summarize evidence rather than copy large raw logs.

# Validation Tools

This directory will contain Python utilities that inspect service state, registration evidence, session state, tunnel interfaces, and connectivity.

The first planned utility is `lab_check.py`, which will generate a concise Markdown report after the basic lab works.

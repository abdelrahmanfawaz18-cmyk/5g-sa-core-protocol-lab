# Scripts

This directory will contain reproducible setup, run, stop, capture, and network-helper scripts developed during later phases.

Scripts must expose errors clearly, document required privileges, and avoid embedding secrets or machine-specific personal paths.

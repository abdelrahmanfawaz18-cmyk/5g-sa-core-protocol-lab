# Tests

This directory will contain automated tests for the project's Python validation tooling.

Tests will be added with the functions they verify so the repository does not contain misleading empty test cases.
